/tmp
*.o
*.a
*.so
.DS_Store
/.claude
xcuserdata/

# Build output (CMake/Xcode/ASan etc.) — covers build, build-cmake, build-asan, build-* variants
/build*/

# Stale standalone test binary (superseded by CMake/Xcode test targets)
/udidx_test
